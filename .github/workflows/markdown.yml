name: "Markdown checks"

on: [push]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
    - uses: articulate/actions-markdownlint@v1
      with:
        files: 'docs/**/*.md'
        version: 0.28.1
